import "tiny-slider/dist/tiny-slider.css"; // Import the Tiny Slider CSS

export default defineNuxtPlugin((nuxtApp) => {
  // The plugin does not register any global components, but ensures CSS is loaded
});
